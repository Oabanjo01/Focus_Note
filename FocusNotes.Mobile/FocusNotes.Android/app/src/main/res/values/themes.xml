<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Your regular app theme - NO splash attributes -->
    <style name="Theme.FocusNote" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Dedicated splash screen theme -->
    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.FocusNote</item>
    </style>
</resources>